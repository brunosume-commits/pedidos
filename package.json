{
  "name": "arte-fogao",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de controle de pedidos - Arte Fogão (Cloudflare Workers + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
